# Copy this file to .env and fill it only on your own computer.
# .env is ignored by Git and must never be uploaded.
TWELVE_DATA_API_KEY=replace_with_your_own_key

# Optional: instead of .env, point to a protected file outside this repository.
# MARKET_WATCHER_CREDENTIALS_FILE=/absolute/path/to/twelve_data.env
